<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apktienda">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NuevoProducto"
            android:label="@string/title_activity_nuevo_producto"></activity>
        <activity
            android:name=".homeVentas"
            android:label="@string/title_activity_home_ventas" />
        <activity
            android:name=".homeCompras"
            android:label="@string/title_activity_home_compras" />
        <activity
            android:name=".home"
            android:label="@string/title_activity_home" />
        <activity android:name=".registerUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>